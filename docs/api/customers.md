# Customers API

Base path: `/api/customers`

The `/api/customers` endpoint is registered but has no active routes yet. Customer creation is handled by `CustomerService.createCustomer()` and is ready for controller integration.
