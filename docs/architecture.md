# Architecture Overview

The application follows a standard Spring MVC layered architecture:

```
HTTP Request
    │
    ▼
Controller  ──►  Service  ──►  Repository  ──►  Database (SQL Server)
    │                │
    │           DTO / Entity
    │               mapping
    ▼
HTTP Response (DTO)
```

| Layer | Package | Responsibility |
|---|---|---|
| **Controllers** | `Controllers/` | Receive HTTP requests, delegate to services, return responses |
| **Services** | `Services/` | Business logic, validation, transaction management |
| **Repositories** | `Repository/` | Data access via Spring Data JPA |
| **Entities** | `Entities/` | JPA-mapped database tables (generated by Hibernate Tools) |
| **DTOs** | `Models/` | Data transfer objects used across the API boundary |
